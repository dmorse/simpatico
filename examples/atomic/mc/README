System:

    800 atoms with WCA (repulsive Lennard-Jones) interactions

Usage:

Monte Carlo (MC) simulation:

    mcSim -e -p param -c commands

Restarting MC simulations

    mcSim -e -r restart -c restart.cmd
