System:

    64000 atoms with cutoff Lennard-Jones interactions (cutoff = 2.5 sigma)

Usage:

NVE md simulation on 8 processors:

    mpirun -np 8 mdSim -e -p param.nve -c commands

Restarting and extending previous simulation:

    mpirun -np 8 mdSim -e -r restart -c commands.rst
