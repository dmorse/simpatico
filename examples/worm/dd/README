System:

    melt of 100 wormlike chains of 8 atoms.

Usage:

Parallel NVE md simulation on 6 processors:

    mpirun -np 6 ddSim -e -p param -c commands

Restarting and extending previous simulation

    mpirun -np 6 ddSim -e -r restart -c restart.cmd
