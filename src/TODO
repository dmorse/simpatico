See util/TODO, mcMd/TODO, and ddMd/TODO for further TODO lists.
This file contains only items that are not localized to a namespace.

Makefiles
---------

- Move all makefile variables to define preprocessor macros to a common 
  file src/config.mk. Include this and compiler.mk into global.mk, have
  other makefiles import global.mk rather than compiler.mk.

- Add python script setOptions to modify common options by editing config.mk
  file (?).

