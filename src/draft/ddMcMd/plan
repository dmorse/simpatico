Create classes:

Namespace DdMcMd
{

  class Simulation : driver for Md, MC and simple hybrid MC simulations
                     Separate commands MC, MD and HMC simulation.

Dummy classes:

  Input and output parameters, and nothing else:

  DdMcMd::AtomStorage
  DdMcMd::GroupStorage
  DdMcMd::Buffer

  // Integrators
  DdMcMd::DdNvtIntegrator
  DdMcMd::DdNveIntegrator

}
