namespace DdMd 
{

   /**
   * \defgroup DdMd_Modifier_Module Modifiers
   * \ingroup  DdMd_Module
   *  
   * Actions that can modify the state of the system.
   *
   * An Modifier object provide operations that modify the state
   * of the system and that can be called every time step, which
   * can thus be used to extend the MD integration algorithm.
   * Each subclass of Modifier can be programmed to take actions
   * at specified intervals (number of time steps) and at specific
   * points within a two-step integration algorithm.
   */

}
