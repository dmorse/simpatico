namespace DdMd 
{

   /**
   * \defgroup DdMd_Modifier_Module Modifiers
   * \ingroup  DdMd_Module
   *  
   * An Modifier object can alter the state of a system during
   * integration, and can thus modify the integration algorithm.
   * Each subclass of modifier can be programmed to take actions
   * at specified intervals (number of time steps) and at specific
   * specific points within a two-step integration algorithm.
   */

}
