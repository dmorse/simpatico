namespace McMd
{

   /**
   * \defgroup McMd_Manager_Module Managers
   * \ingroup  McMd_Module 
   *
   * A Manager is a container for objects with a common base class.
   *
   * A Manager is used to create and and manage a list of objects that
   * instances of different subclasses of a common base class, and to 
   * create objects of different subclasses in response to commands 
   * given in a parameter file.
   */

}
